# secrets
.env
.env.*
!.env.example

# deps & build
node_modules/
dist/
build/
.vite/
coverage/

# runtime artifacts
# working state: recordings, runs, output audio, ledger.jsonl (PRD §7).
# results/ is committed and is what the write-up cites.
data/
*.log
benchmark-results/*.json
!benchmark-results/.gitkeep

# os / editor
.DS_Store
.idea/
.vscode/
*.swp

# tdd state
.tdd/worktrees/

# qa artifacts
.qa/screens/
node_modules

# Operational backups pulled from a deployed instance (ticket 072) — large WAVs,
# and never the source of truth. data/ is.
backups/
