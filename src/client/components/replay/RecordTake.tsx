/**
 * Ticket 036 — STUB. The record-a-take flow behind [data-record-new].
 *
 * The DOM contract this must satisfy is documented in
 * src/client/views/ReplayView.record.test.tsx. Nothing is implemented yet.
 */

import type { ReactElement } from 'react';

export interface RecordTakeProps {
  [key: string]: unknown;
}

export default function RecordTake(_props: RecordTakeProps): ReactElement | null {
  return null;
}
